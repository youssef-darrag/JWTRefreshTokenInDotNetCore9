# JWT Refresh Token 

Adding Refresh Token support to an API project that uses JWT Authentication. This improves security and session management by allowing users to securely obtain a new Access Token when the old one expires, without requiring them to log in every time.

---
